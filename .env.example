# Google Sheet to use as database
GOOGLE_SHEET_ID=your_google_sheet_id

# Worksheet/tab name inside the spreadsheet
SHEET_NAME=Sheet1

# Optional for cloud deployment (Render): JSON string for service account credentials
# GOOGLE_CREDENTIALS={"type":"service_account",...}
